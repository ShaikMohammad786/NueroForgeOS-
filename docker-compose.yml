version: "3.8"

services:
  runner:
    build: ./runner
    container_name: neuroforge-runner
    ports:
      - "8001:8001"
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
